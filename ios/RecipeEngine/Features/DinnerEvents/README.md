# Dinner Events

Views/view models for hosting a dinner (multi-dish, multi-guest), dish claiming, RSVP,
and calendar (`.ics`) export. Maps to `apps.social` on the backend
(`/api/v1/social/events/`, `/api/v1/social/dishes/<id>/claim/`).
