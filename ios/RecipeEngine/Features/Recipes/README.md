# Recipes

Views/view models for recipe list/search, detail, create/edit, comments, "I made this"
posts, and collections. Maps to `apps.recipes` on the backend
(`GET/POST /api/v1/recipes/recipes/`, `/api/v1/recipes/collections/`,
`/api/v1/recipes/vocabulary/`).
