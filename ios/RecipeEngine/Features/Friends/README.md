# Friends

Views/view models for the friend list (add by username, remove). One-directional,
unilateral — no accept/decline step, matches the backend model. Maps to `apps.accounts`
(`/api/v1/accounts/friends/`).
