# Models

Codable structs mirroring the component schemas in `../../../api/schema.yaml`
(`Recipe`, `DinnerEvent`, `EventDish`, `EventInvite`, `Friendship`, `User`, etc.). Keep
these in sync with the schema rather than hand-copying from Django serializer source.
